import { lazy } from 'react';

export const EmailField = lazy(() => import('./EmailField'));
export const PasswordField = lazy(() => import('./PasswordField'));
export const TextField = lazy(() => import('./TextField'));

